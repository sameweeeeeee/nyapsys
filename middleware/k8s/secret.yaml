apiVersion: v1
kind: Secret
metadata:
  name: nyapsys-secrets
  namespace: nyapsys
type: Opaque
stringData:
  api-secret-key: "REPLACE_WITH_OPENSSL_RAND_HEX_32_OUTPUT"
  # Apply manually: kubectl apply -f middleware/k8s/secret.yaml